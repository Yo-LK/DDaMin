import 'package:flutter/material.dart';
class RelocalizationScreen extends StatelessWidget {
  const RelocalizationScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Relocalization')));
}
